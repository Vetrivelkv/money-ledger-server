PORT=5000
CLIENT_ORIGIN=http://localhost:5173

RDB_HOST=127.0.0.1
RDB_PORT=28015
RDB_DB=money_ledger

JWT_SECRET=change_this_to_long_random_string

# Session defaults (20 minutes with sliding refresh)
SESSION_TTL_MINUTES=20
SESSION_REFRESH_THRESHOLD_MINUTES=5
# Optional override (otherwise derived from SESSION_TTL_MINUTES)
# JWT_EXPIRES_IN=20m
